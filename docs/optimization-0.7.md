# 0.7 余晖远征优化

目标：完成美术、战斗反馈、构筑成长和单局选择的一轮整体优化，交付可直接本地试玩的 Web 版本。

## 验收范围

- 构筑：现有成长从展示接入真实伤害、辅助和废料规则；成熟的链式、火力、支援编组具有不同爆发机制，暂停、换位、重开与弹道快照保持正确。
- 玩法：站点奖励和风险明确，装配显示编组前后变化；一局有近期成长目标、成型回报和有用的失败复盘。保留五槽和实际相邻联动。
- 美术：沿用余晖防线手绘方向，优化战场地表、列车存在感、构筑特效和 HUD 层次，与现有工坊/站点美术衔接；手机画面不重叠、不遮主要操作。
- 区域：12 个主题，每区 60 秒；完整左右刷怪通道保持全宽开放。地域特色由地面材质、嵌入式遗迹和光影形成，避免把场景道具堆在两侧。
- 局外：3 款不同主动核心的车头、4 项整备选择、基于已有游玩记录的模块解锁和本地保存；车头在战斗中实际出现并发动技能。
- 敌人：9 种独立轮廓与行为提示，旧敌人同步换图；攻击、元素受击、护盾与构筑爆发对应实际规则。
- 文字：加载项目自带标题、正文字体；伤害数字采用原创 Afterglow Wake v6 字族，替换用户否定的 v2 墨笔字及区分度不足的 v3，按五种情景区别字重、颜色、标记与运动，检查整串排版和手机尺寸。
- 反馈：爆发、受击与精英蓄力使用清楚而有限的视觉和声音提示；关闭镜头动态时保持可读。
- 稳定性：核心规则、面板、站点与开局专项测试、类型检查、实际 Web 构建；真实浏览器点击覆盖开局、装车、站点、换位、暂停、重开，并检查成熟构筑和结算画面。
- 节奏：相同种子与策略的优化前后模拟，另以代表构筑诊断成型效果；结果仅是开发策略证据，不宣称真人留存或抖音真机通过。

## 实施状态

正在实现。原始工作区改动保留。对照源与验证产物保存在 `artifacts/optimization-20260910/`，完成后在本页补充实际结果和限制。


## 2026-09-10 当前刷怪回归证据

当前源码执行 `node --experimental-strip-types tools/check-spawn-pacing.mjs artifacts/optimization-20260910/current-control-pacing-v1`。源码哈希、逐局选择和完整结果保存在该目录的 report.json；两臂均使用水面浮出机制，仅切换分批或逐只释放。

同种子1–12、三款车头、无模块、balanced脚本策略、每局180秒，共72局：

| 车头 | 逐只刷怪存活 | 分批刷怪存活 |
| --- | --- | --- |
| 破晓 | 8/12 | 8/12 |
| 鸣雷 | 11/12 | 11/12 |
| 守望 | 12/12 | 12/12 |

首分钟前的补给选择配对差异为0。三款车头的平均时长差异小于0.32秒。本轮保留当前分批参数；该样本不证明车头间平衡，也不证明真人趣味或难度已达标。

当前范围仍未全部完成：12区共用余晖城市背景，不能计为12区独立美术交付。上文的左右通道方案已由用户后续认可的水面浮出方案替代；区域差异化目标仍保留。已有远征记录、词条详情、存盘失败提示和系统减少动态偏好支持，但这些不替代区域美术与完整体验验收。

## 区域美术接入更新

现已生成并接入全部12张 `lastlight-{region}-v1.png` 独立区域背景，资源哈希与导入UUID见 `artifacts/optimization-20260910/region-assets-v1/manifest.json`。保持水面浮出机制；盐原、沙海、灰原等使用对应地貌包围的开放水道。

前10区完成390×844定点画面与区域切换检查。最后两区（极光冻湖、失落天港）素材已检查、类型检查与Web构建通过，12区规则测试通过；浏览器工具连接中断，最后两区的运行时叠加及全12区独立帧循环检查尚未完成，不计为完整区域验收通过。

## 背景加载压力调整

12张源PNG合计31.43 MiB，941×1672尺寸按RGBA展开约72.02 MiB（不含mipmap及其他资源，非实测显存）。启动改为仅请求城市背景；游戏中请求当前区和下一区，成功加载后缓存。降低初始请求量，不声称降低跑完全12区后的峰值占用。资源包总大小不变。

实际 requestNearbyTerrain 方法通过模拟异步Cocos加载器检查：首区仅追加货场、重复调用不重复请求、进入货场预读白霜、末区回绕复用城市、销毁后忽略回调。类型检查和Web构建通过。浏览器连接仍不可用，实际网络加载时序及末两区画面仍待验证。
